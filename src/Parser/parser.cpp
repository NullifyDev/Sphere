
/*#include <iostream>
#include <vector>

#include "../lexer/tokens/tokens.hpp"

std::string Parse(const std::vector<Tokens::Token>& tokens) {
    std::stringstream preload;
    for (int i = 0; i < tokens.size(); i++) {
        switch(tokens.at(i).type) {
            case Tokens::Type::out:
            case Tokens::Type::outln:

        }
    }
}*/
